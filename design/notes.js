/*
    let movements = [] //array con tutti i movimenti

    let movement =
    {
        id: index
        name: "nome"; // nome del movimento;
        date: "date"; // data del movimento;
        inbound: false; // è un movimento in entrata?
        outgoing: false; // è un movimento in uscita?
    }
*/

/*
    function setState(){
        if(button.inbound is pressed){
            movement.inbound = true;
        }

        if(button.outgoing is pressed){
            movement.outgoing = true;
        }
    }
*/

/*
    // (onclick) => button aggiungi;

    function pushMovement(){
        movements.push(
            {
                id: index
                name: "nome";
                date: "date";
                inbound: true / false;
                outgoing: false / true;
            }
        )
    }
*/

/*
    DISPLAY MOVEMENTS ITEMS IN TABLE:
*/